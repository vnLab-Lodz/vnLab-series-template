import { StyledTypeProps } from "~types"
import styled from "styled-components"

export const em = styled.em<StyledTypeProps>`
  font-family: inherit;
  font-size: inherit;
  font-style: italic;
  font-weight: inherit;
  line-height: inherit;
  color: inherit;
`
